# 📖 Справочник Rclone Manager

Быстрый поиск команд, параметров и решений.

---

## 🔍 Быстрый поиск

### Поиск по командам
- [Управление облаками](#управление-облаками)
- [Системные команды](#системные-команды)
- [Команды rclone](#команды-rclone)

### Поиск по задачам
- [Установка](#установка)
- [Настройка](#настройка)
- [Безопасность](#безопасность)
- [Отладка](#отладка)

---

## 🎯 Управление облаками

### Основные команды rclone_manager.sh

| Команда | Описание | Безопасность |
|---------|----------|--------------|
| `rclone_manager.sh ro [облако]` | Подключить в read-only | ✅ Безопасно |
| `rclone_manager.sh rw [облако]` | Подключить в read-write | ⚠️ Опасно |
| `rclone_manager.sh toggle [облако]` | Переключить режим | ⚠️ Зависит от текущего |
| `rclone_manager.sh st [облако]` | Показать статус | ✅ Безопасно |
| `rclone_manager.sh stop [облако]` | Остановить облако | ✅ Безопасно |

**Примечание:** Без указания облака команды работают со всеми облаками.

### Алиасы (добавляются в ~/.bashrc)

```bash
# Базовые команды
rmro              # Все облака в read-only
rmrw              # Все облака в read-write
rmt               # Переключить режим всех
rms               # Статус всех
rmstop            # Остановить все

# Для конкретных облаков
rmro_yandex       # Яндекс Диск в read-only
rmrw_yandex       # Яндекс Диск в read-write
rmt_yandex        # Переключить Яндекс Диск
rms_yandex        # Статус Яндекс Диска

rmro_mail         # Mail.ru в read-only
rmrw_mail         # Mail.ru в read-write
rmt_mail          # Переключить Mail.ru
rms_mail          # Статус Mail.ru
```

---

## ⚙️ Системные команды

### Установка и настройка

```bash
# Установка
sudo ./install.sh

# Проверка установки
rclone_manager.sh st

# Перезагрузка systemd
systemctl --user daemon-reload

# Проверка конфигурации rclone
rclone config show
```

### Управление systemd

```bash
# Статус всех сервисов
systemctl --user list-units 'rclone@*'

# Статус конкретного сервиса
systemctl --user status rclone@yandex_disk-ro.service

# Запуск вручную
systemctl --user start rclone@yandex_disk-ro.service

# Остановка вручную
systemctl --user stop rclone@yandex_disk-ro.service

# Перезапуск
systemctl --user restart rclone@yandex_disk-ro.service

# Просмотр логов
journalctl --user -u rclone@yandex_disk-ro.service -f
```

### Работа с файлами

```bash
# Проверка точек монтирования
findmnt --target ~/mnt/yandex_disk

# Проверка смонтированных дисков
mount | grep rclone

# Проверка свободного места
df -h ~/mnt/yandex_disk

# Просмотр файлов
ls -la ~/mnt/yandex_disk
```

---

## 📂 Команды rclone

### Базовые команды проверки

```bash
# Версия rclone
rclone version

# Список всех remote
rclone listremotes

# Проверить список папок в remote
rclone lsd yandex_disk:
rclone lsd mail_ru:

# Просмотреть файлы в папке
rclone ls yandex_disk:путь/к/папке

# Рекурсивный список файлов
rclone lsf -R yandex_disk:путь
```

### Копирование и синхронизация

```bash
# Скачать с облака (без удаления)
rclone copy yandex_disk:путь/в/облаке /локальный/путь --progress

# Синхронизировать (облако → локаль, удалит лишнее)
rclone sync yandex_disk:путь/в/облаке /локальный/путь --progress

# Тест перед синхронизацией (обязательно!)
rclone sync yandex_disk:путь/в/облаке /локальный/путь --dry-run --progress

# Загрузить на облако
rclone copy /локальный/путь yandex_disk:путь/в/облаке --progress

# Синхронизировать (локаль → облако)
rclone sync /локальный/путь yandex_disk:путь/в/облаке --progress
```

### Проверка и валидация

```bash
# Посмотреть, что на облаке
rclone ls yandex_disk:"Backups/Project"

# Сравнить локал и облако
rclone check /home/user/Project "yandex_disk:Backups/Project"

# Проверить статистику
rclone rc core/stats

# Очистить кэш вручную
rm -rf ~/.cache/rclone/*

# Принудительно обновить кэш директорий
rclone rc vfs/refresh recursive=true remote=yandex_disk:
```

---

## 🔧 Параметры VFS

### Режимы кэширования

| Режим | Команда | Когда использовать |
|-------|---------|-------------------|
| **off** | `--vfs-cache-mode off` | Только чтение, стриминг |
| **minimal** | `--vfs-cache-mode minimal` | Редкое редактирование |
| **writes** | `--vfs-cache-mode writes` | Работа с документами |
| **full** | `--vfs-cache-mode full` | Медиа, сложные редакторы |

### Основные параметры

```bash
--vfs-cache-mode full          # Режим кэширования
--vfs-cache-max-size 5G        # Максимальный размер кэша
--vfs-read-chunk-size 64M      # Размер чанка для чтения
--vfs-read-ahead 256M          # Предзагрузка данных
--buffer-size 64M              # Размер буфера
--dir-cache-time 168h          # Кэш директорий
--attr-timeout 1s              # Кэш атрибутов
--allow-other                  # Доступ другим пользователям
--read-only                    # Только чтение
--log-level INFO               # Уровень логирования
--log-file /путь/к/log.log    # Файл логов
```

---

## 🛡️ Безопасность

### Золотые правила

1. **Всегда начинайте с read-only**
   ```bash
   rmro
   rms
   ```

2. **Проверяйте статус перед операциями**
   ```bash
   rms
   ```

3. **Переключайтесь только когда нужно**
   ```bash
   rmt yandex_disk  # Только если точно нужно
   ```

4. **Немедленно возвращайтесь в read-only**
   ```bash
   rmt yandex_disk  # Вернуть обратно
   ```

### Что НЕ делать

```bash
# ❌ НИКОГДА:
rm -rf ~/mnt/yandex_disk/важная_папка
rmrw без необходимости
Игнорировать статус перед важными операциями
```

### Безопасные сценарии

```bash
# Чтение (100% безопасно)
rmro
rms
ls ~/mnt/
cp ~/mnt/file ~/Downloads/

# Редактирование (контролируемый риск)
rmro
rms
rmt yandex_disk    # Переключить
# ... редактировать ...
rmt yandex_disk    # Вернуть
rms                # Проверить
```

---

## 🚨 Отладка

### Проверка установки

```bash
# Проверка rclone
rclone version

# Проверка конфигурации
rclone config show

# Проверка systemd
systemctl --user list-units 'rclone@*'
```

### Проверка работы

```bash
# Статус
rms
rms yandex_disk

# Проверка монтирования
findmnt --target ~/mnt/yandex_disk
mount | grep rclone

# Проверка файлов
ls -la ~/mnt/yandex_disk
```

### Просмотр логов

```bash
# Логи всех облаков
ls ~/.cache/rclone/

# Лог конкретного облака
tail -f ~/.cache/rclone/rclone-yandex_disk-ro.log

# Логи systemd
journalctl --user -u rclone@yandex_disk-ro.service -f
```

### Распространенные проблемы

| Проблема | Решение |
|----------|---------|
| **Файлы не видны** | `sleep 3 && ls ~/mnt/` |
| **Permission denied** | Проверьте `/etc/fuse.conf` |
| **Сервисы не запускаются** | `systemctl --user daemon-reload` |
| **Токен устарел** | `rclone config update yandex_disk` |
| **Медленная работа** | Увеличьте кэш: `--vfs-cache-max-size 10G` |

---

## 📋 Установка и настройка

### Быстрая установка

```bash
# 1. Установка
cd ~/projects/rclone-manager
sudo ./install.sh

# 2. Настройка rclone
rclone config
# yandex_disk (Yandex)
# mail_ru (Mail.ru)

# 3. Первый запуск
rmro
rms

# 4. Проверка
ls ~/mnt/yandex_disk
```

### Ручная установка

```bash
# Создание директорий
mkdir -p ~/.config/systemd/user
mkdir -p ~/mnt/yandex_disk
mkdir -p ~/mnt/mail_ru

# Установка скриптов
cp rclone_manager.sh /usr/local/bin/
chmod +x /usr/local/bin/rclone_manager.sh

# Установка сервисов
cp rclone@-ro.service ~/.config/systemd/user/
cp rclone@-rw.service ~/.config/systemd/user/

# Перезагрузка systemd
systemctl --user daemon-reload

# Добавление алиасов
echo "
alias rmro='rclone_manager.sh ro'
alias rmrw='rclone_manager.sh rw'
alias rmt='rclone_manager.sh toggle'
alias rms='rclone_manager.sh st'
" >> ~/.bashrc

source ~/.bashrc
```

---

## 🎯 Поддерживаемые облака

### Yandex Disk
- **Имя remote:** `yandex_disk`
- **Тип:** `yandex`
- **Особенности:** Стандартный API, хорошая скорость

### Mail.ru Cloud
- **Имя remote:** `mail_ru`
- **Тип:** `mailru`
- **Особенности:** Требует пароль для внешних приложений
- **speedup_enable:** `false` (рекомендуется)

---

## 🔗 Полезные ссылки

### Внутренние
- **[Быстрый старт](Быстрый%20старт.md)** - Пошаговая установка
- **[Безопасность](Безопасность.md)** - Правила работы
- **[Системные сервисы](Системные%20сервисы.md)** - Настройка systemd
- **[Параметры VFS](Параметры%20VFS.md)** - Настройки производительности
- **[Готовые сценарии](../examples/usage_examples.md)** - Примеры использования

### Внешние
- [Официальная документация rclone](https://rclone.org/)
- [Документация по Yandex Disk](https://rclone.org/yandex/)
- [Документация по Mail.ru Cloud](https://rclone.org/mailru/)

---

## 💡 Быстрые команды

### Безопасные (всегда используйте)
```bash
rmro              # Подключить в безопасном режиме
rms               # Проверить статус
rmstop            # Остановить все
ls ~/mnt/         # Просмотр файлов
cp ~/mnt/ ~/Downloads/  # Копирование
```

### Для записи (используйте осторожно)
```bash
rmrw              # Подключить в режим записи
rmt               # Переключить режим
nano ~/mnt/file   # Редактирование
```

### Системные
```bash
systemctl --user status rclone@*  # Статус сервисов
journalctl --user -u rclone@yandex_disk-ro.service -f  # Логи
rclone config show  # Конфигурация
```

---

## 📞 Поддержка

### Если что-то не работает:

1. **Проверьте установку:** `rclone version`
2. **Проверьте конфигурацию:** `rclone config show`
3. **Проверьте статус:** `rms`
4. **Посмотрите логи:** `tail -f ~/.cache/rclone/rclone-*.log`
5. **Перезапустите:** `rmstop && rmro`

---

**Быстрый доступ:** Используйте `Ctrl+F` для поиска по этой странице.