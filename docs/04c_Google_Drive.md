# ☁️ Настройка Google Drive с собственным API Client ID

**Пошаговая инструкция по созданию собственного проекта в Google Cloud Console (новый интерфейс Google Auth Platform) для максимальной скорости работы rclone.**

---

## ⚠️ Зачем это нужно?
По умолчанию rclone использует общий Client ID, который делят все пользователи rclone в мире. Это часто приводит к:
- **Низкой скорости** загрузки/скачивания (ограничения Google)
- **Ошибкам 403 Rate Limit Exceeded**
- Нестабильной работе в пиковые часы

Создание собственного Client ID полностью решает эти проблемы.

---

## 🛠️ Пошаговая инструкция

### Шаг 1: Создание проекта
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/).
2. Если у вас нет аккаунта Google Cloud, зарегистрируйтесь (это бесплатно).
3. Нажмите на список проектов вверху слева (обычно там написано "Select a project").
4. Нажмите **"New Project"**.
5. Введите имя (например, `rclone-drive`) и нажмите **"Create"**.

### Шаг 2: Включение Google Drive API
1. Убедитесь, что выбран ваш новый проект.
2. Откройте меню (три полоски слева) -> **"APIs & Services"** -> **"Enabled APIs & services"**.
3. Нажмите кнопку **"+ ENABLE APIS AND SERVICES"** вверху.
4. В поиске введите `Google Drive API`.
5. Выберите его и нажмите **"Enable"**.

### Шаг 3: Настройка разрешений и доступа (Google Auth Platform)
Google обновил интерфейс, теперь все настройки находятся в разделе **"Google Auth Platform"** (или **"Branding" / "Audience" / "Data Access"**).

1. Перейдите в **"APIs & Services"** -> **"OAuth consent screen"** (или сразу в "Google Auth Platform").
2. **Branding** (Экран согласия):
   - Укажите **App name**: `rclone` (или любое другое).
   - Укажите **User support email** и **Developer contact information** (ваш email).
   - Сохраните изменения.
3. **Data Access** (Scopes / Области доступа):
   - Перейдите в раздел **"Data Access"**.
   - Нажмите **"Add or remove scopes"**.
   - Найдите в списке `.../auth/drive` (Google Drive API, full access).
   - Поставьте галочку и нажмите **"Update"**.
4. **Audience** (Test Users / Тестовые пользователи):
   - Перейдите в раздел **"Audience"**.
   - **ВАЖНО!** В блоке "Test users" нажмите **"+ ADD USERS"**.
   - Введите свой Gmail адрес.
   - Нажмите **"Save"**. Без этого шага вы не сможете авторизоваться!

### Шаг 4: Создание ключей (Credentials)
1. Перейдите в раздел **"Clients"** (или **"Credentials"** в старом меню).
2. Нажмите **"+ CREATE CLIENT"** (или **"+ CREATE CREDENTIALS"** -> **"OAuth client ID"**).
3. **Application type**: выберите **"Desktop app"**.
4. **Name**: оставьте как есть или введите `rclone client`.
5. Нажмите **"Create"**.
6. Появится окно с вашими ключами:
   - **Client ID** (длинная строка, заканчивается на `.apps.googleusercontent.com`)
   - **Client Secret** (секретная строка)
7. **Не закрывайте это окно** или скопируйте ключи в блокнот.

---

## ⚙️ Настройка rclone

Теперь используйте полученные ключи при настройке rclone.

1. Запустите конфигурацию:
   ```bash
   rclone config
   ```
2. Создайте новый remote (`n`) -> имя `google_drive` -> тип `drive`.
3. Когда спросят `client_id`, вставьте ваш **Client ID** из шага 4.
4. Когда спросят `client_secret`, вставьте ваш **Client Secret** из шага 4.
5. `Scope`: выберите **1** (Full access).
6. `root_folder_id` и `service_account_file`: оставьте пустыми (Enter).
7. `Edit advanced config?`: **n** (No).
8. `Use web browser to automatically authenticate?`: **y** (Yes).
9. В браузере выберите свой аккаунт.
   - ⚠️ **Внимание:** Google выдаст предупреждение "Google hasn’t verified this app".
   - Нажмите **"Advanced"** -> **"Go to rclone (unsafe)"**.
   - Нажмите **"Continue"** / **"Allow"**.
10. `Configure this as a Shared Drive?`: **n** (No).

**Готово!** Теперь ваш Google Drive подключен с максимальной производительностью.

---

## 🔗 Полезные ссылки
- [Официальная документация rclone по Google Drive](https://rclone.org/drive/#making-your-own-client-id)