# 📋 Команды Rclone Manager

## 🎯 Основные команды управления

### Команды для всех облаков

| Команда | Описание | Безопасность | Алиас |
|---------|----------|--------------|-------|
| `rclone_manager.sh ro` | Все облака в read-only | ✅ Безопасно | `rmro` |
| `rclone_manager.sh rw` | Все облака в read-write | ⚠️ Опасно | `rmrw` |
| `rclone_manager.sh toggle` | Переключить режим всех облаков | ⚠️ Зависит от текущего | `rmt` |
| `rclone_manager.sh st` | Статус всех облаков | ✅ Безопасно | `rms` |
| `rclone_manager.sh stop` | Остановить все облака | ✅ Безопасно | `rmstop` |

### Команды для конкретных облаков

Формат: `rclone_manager.sh <режим> <имя_облака>`

**Примеры для Yandex Disk:**
```bash
rclone_manager.sh ro yandex_disk    # В read-only
rclone_manager.sh rw yandex_disk    # В read-write
rclone_manager.sh toggle yandex_disk # Переключить режим
rclone_manager.sh st yandex_disk    # Статус
rclone_manager.sh stop yandex_disk  # Остановить
```

**Алиасы для конкретных облаков:**
```bash
rmro_yandex       # Яндекс Диск в read-only
rmrw_yandex       # Яндекс Диск в read-write
rmt_yandex        # Переключить Яндекс Диск
rms_yandex        # Статус Яндекс Диска
rmstop_yandex     # Остановить Яндекс Диск

rmro_mail         # Mail.ru в read-only
rmrw_mail         # Mail.ru в read-write
rmt_mail          # Переключить Mail.ru
rms_mail          # Статус Mail.ru
rmstop_mail       # Остановить Mail.ru

rmro_google       # Google Drive в read-only
rmrw_google       # Google Drive в read-write
rmt_google        # Переключить Google Drive
rms_google        # Статус Google Drive
rmstop_google     # Остановить Google Drive
```

---

## 🛡️ Безопасные рабочие потоки

### Поток 1: Чтение файлов (100% безопасно)
```bash
rmro              # Подключить в безопасном режиме
rms               # Проверить статус
ls ~/mnt/         # Работать с файлами
cp ~/mnt/file ~/Downloads/  # Копировать
```

### Поток 2: Редактирование файлов (контролируемый риск)
```bash
# 1. Начать с безопасного режима
rmro
rms

# 2. Переключить только если нужно
rmt yandex_disk

# 3. Редактировать файлы
nano ~/mnt/yandex_disk/notes.txt

# 4. Немедленно вернуться в безопасный режим
rmt yandex_disk

# 5. Проверить возврат
rms yandex_disk
```

### Поток 3: Массовые операции
```bash
# Подключение всех облаков
rmro
rms

# Если нужно редактировать во всех - переключаем все
rmrw
rms

# Работа...

# Возврат в безопасный режим
rmro
rms
```

---

## 🔍 Команды отладки и проверки

### Проверка статуса
```bash
rms               # Статус всех облаков
rms yandex_disk   # Статус конкретного облака
```

### Просмотр логов
```bash
# Логи всех облаков
ls ~/.cache/rclone/

# Лог конкретного облака
tail -f ~/.cache/rclone/rclone-yandex_disk-ro.log
```

### Проверка systemd
```bash
# Статус всех сервисов
systemctl --user list-units 'rclone-*@*'

# Статус конкретного сервиса
systemctl --user status rclone-ro@yandex_disk.service

# Просмотр логов сервиса
journalctl --user -u rclone-ro@yandex_disk.service -f
```

### Проверка монтирования
```bash
# Проверить что монтирование активно
findmnt --target ~/mnt/yandex_disk

# Проверить все монтирования rclone
mount | grep rclone

# Проверить свободное место
df -h ~/mnt/yandex_disk
```

---

## 📂 Команды rclone (встроенные)

### Базовые команды проверки
```bash
# Версия rclone
rclone version

# Список всех remote
rclone listremotes

# Проверить содержимое облака
rclone lsd yandex_disk:
rclone lsd mail_ru:

# Просмотреть файлы в папке
rclone ls yandex_disk:путь/к/папке
```

### Копирование и синхронизация
```bash
# Скачать с облака (без удаления)
rclone copy yandex_disk:путь/в/облаке /локальный/путь --progress

# Синхронизировать (облако → локаль, удалит лишнее)
rclone sync yandex_disk:путь/в/облаке /локальный/путь --progress

# Тест перед синхронизацией (обязательно!)
rclone sync yandex_disk:путь/в/облаке /локальный/путь --dry-run --progress

# Загрузить на облако
rclone copy /локальный/путь yandex_disk:путь/в/облаке --progress
```

### Проверка и валидация
```bash
# Посмотреть, что на облаке
rclone ls yandex_disk:"Backups/Project"

# Сравнить локал и облако
rclone check /home/user/Project "yandex_disk:Backups/Project"

# Проверить статистику
rclone rc core/stats

# Очистить кэш вручную
rm -rf ~/.cache/rclone/*

# Принудительно обновить кэш директорий
rclone rc vfs/refresh recursive=true remote=yandex_disk:
```

---

## 💡 Алиасы для удобства

Добавьте в `~/.bashrc`:

```bash
# Безопасные команды (всегда используйте их)
alias safe_read='rmro && rms'      # Подключить и проверить
alias safe_check='rms'             # Быстрая проверка
alias safe_stop='rmstop'           # Остановить все

# Для записи (используйте осознанно)
alias edit_mode='rmt && rms'       # Переключить и проверить

# Быстрый старт
alias start_safe='rmro && rms && echo "✅ Безопасный режим активен"'

# Экстренная остановка
alias emergency_stop='rmstop && echo "🚨 Все облака остановлены"'
```

---

## 🚨 Что НЕ делать

```bash
# ❌ НИКОГДА не делайте так:
rm -rf ~/mnt/yandex_disk/важная_папка
# В read-write режиме = УДАЛЕНИЕ ИЗ ОБЛАКА БЕЗ КОРЗИНЫ!

# ❌ НИКОГДА не запускайте без проверки:
rmrw              # Без необходимости

# ❌ НИКОГДА не игнорируйте статус:
# Перед важной операцией всегда проверяйте:
rms
```

---

## 📞 Что делать, если...

### Случайно переключил в read-write?
```bash
# 1. Немедленно вернитесь в read-only
rmt yandex_disk

# 2. Проверьте что не удалили ничего важного
ls ~/mnt/yandex_disk

# 3. Если удалили - проверьте облако через веб-интерфейс
# (восстановление возможно только если облако поддерживает корзину)
```

### Не помните в каком режиме?
```bash
rms               # Покажет текущий режим
```

### Нужно срочно остановить все?
```bash
rmstop            # Остановить все облака
```

### Файлы не видны после запуска?
```bash
# Подождите 2-3 секунды
sleep 3
ls ~/mnt/yandex_disk

# Проверьте логи
tail -f ~/.cache/rclone/rclone-yandex_disk-ro.log
```

### Ошибка аутентификации?
```bash
# Проверьте конфигурацию
rclone config show

# Обновите токен
rclone config update yandex_disk
```

---

## 📚 Связанные документы

- **[Безопасность](02_Безопасность.md)** - Правила безопасной работы
- **[VFS параметры](06_VFS_Параметры.md)** - Настройки производительности
- **[Системные сервисы](05_Системные_сервисы.md)** - Настройка systemd
- **[Быстрый старт](01_Быстрый_старт.md)** - Пошаговая установка
- **[Справочник](07_Справочник.md)** - Быстрый поиск информации
- **[Готовые сценарии](../examples/usage_examples.md)** - Примеры использования

---

**Главное правило: Если сомневаетесь - используйте read-only!** 🔒