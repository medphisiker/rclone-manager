# 📊 VFS параметры Rclone Manager

**Единый источник правды для настроек производительности.**

---

## 🎯 Быстрый выбор режима

| Сценарий | Режим VFS | Кэш | Чанк | Предзагрузка | Пример |
|----------|-----------|-----|------|--------------|--------|
| **Только чтение** | `full` | 10G | 32M | 128M | Быстрое чтение |
| **Документы** | `writes` | 2G | 32M | 128M | Редактирование файлов |
| **Медиа** | `full` | 10G | 128M | 512M | Видео, музыка |
| **Бэкапы** | `minimal` | 1G | 256M | 512M | Загрузка больших файлов |
| **По умолчанию** | `full` | 5G | 64M | 256M | Универсальный |

---

## ⚙️ Все параметры VFS

### Основные параметры

| Параметр | Значение по умолчанию | Описание | Рекомендация |
|----------|----------------------|----------|--------------|
| `--vfs-cache-mode` | `off` | Режим кэширования | `writes` или `full` |
| `--vfs-cache-max-size` | не ограничено | Максимальный размер кэша | `5G` - `10G` |
| `--vfs-cache-max-age` | `1h` | Время хранения в кэше | `720h` (30 дней) |
| `--vfs-read-chunk-size` | `128M` | Размер чанка для чтения | `64M` - `128M` |
| `--vfs-read-chunk-size-limit` | `off` | Лимит чанков на файл | `2048M` (2 ГБ) |
| `--vfs-read-ahead` | `0M` | Предзагрузка данных | `256M` - `512M` |
| `--buffer-size` | `16M` | Размер буфера в памяти | `64M` для скорости |
| `--dir-cache-time` | `5m` | Кэш списка директорий | `168h` (7 дней) |
| `--attr-timeout` | `1s` | Кэш атрибутов файлов | `1s` - `5s` |

### Дополнительные параметры

| Параметр | Значение | Описание |
|----------|----------|----------|
| `--read-only` | - | Явный запрет записи |
| `--no-checksum` | - | Без проверки контрольных сумм |
| `--no-modtime` | - | Без обновления времени модификации |
| `--allow-other` | - | Разрешает доступ другим пользователям |
| `--log-level` | `INFO` | Уровень логирования |
| `--log-file` | путь | Файл для логов |

---

## 📋 Готовые конфигурации

### Конфигурация 1: Read-only (безопасный режим с кешированием)
```bash
rclone mount %i: /home/%u/mnt/%i \
    --allow-other \
    --read-only \
    --vfs-cache-mode full \
    --vfs-cache-max-size 10G \
    --vfs-read-chunk-size 32M \
    --vfs-read-ahead 128M \
    --buffer-size 32M \
    --dir-cache-time 24h \
    --attr-timeout 1h \
    --no-checksum \
    --no-modtime \
    --log-level INFO \
    --log-file /home/%u/.cache/rclone/rclone-%i-ro.log
```

**Когда использовать:**
- Только чтение файлов с максимальной скоростью
- Просмотр медиа (видео, музыка) с поддержкой перемотки
- Работа с документами в режиме просмотра
- Максимальная безопасность при высокой производительности

**Плюсы:**
- ✅ Максимальная безопасность (флаг `--read-only`)
- ✅ Высокая скорость доступа за счет кеширования (10 ГБ)
- ✅ Плавная перемотка в медиафайлах
- ✅ Длительное кеширование структуры директорий (24 часа)

**Минусы:**
- ❌ Занимает до 10 ГБ на локальном диске под кеш
- ❌ Требуется время на первый прогрев кеша

---

### Конфигурация 2: Read-write (режим записи)
```bash
rclone mount %i: /home/%u/mnt/%i \
    --allow-other \
    --vfs-cache-mode full \
    --vfs-cache-max-size 5G \
    --vfs-read-chunk-size 64M \
    --vfs-read-ahead 256M \
    --buffer-size 64M \
    --no-checksum \
    --no-modtime \
    --log-level INFO \
    --log-file /home/%u/.cache/rclone/rclone-%i-rw.log
```

**Когда использовать:**
- Редактирование файлов
- Запись документов
- Работа с медиа
- Совместимость с приложениями

**Плюсы:**
- ✅ Полная совместимость с приложениями
- ✅ Безопасная запись
- ✅ Быстрый seek в больших файлах
- ✅ Работа с медиаплеерами

**Минусы:**
- ❌ Большое потребление диска
- ❌ Может добавить задержки
- ❌ Требует места для кэша

---

## 🔧 Подробное сравнение режимов VFS

### 1. `--vfs-cache-mode full` (в режиме Read-Only)
**Описание:** Полное кеширование для режима чтения. Файлы кешируются локально для мгновенного доступа при повторном открытии.

**Параметры:**
```bash
--vfs-cache-mode full
--vfs-cache-max-size 10G
--vfs-read-ahead 128M
--dir-cache-time 24h
```

**Когда использовать:**
- ✅ Основной режим для повседневной работы
- ✅ Стриминг медиа с перемоткой
- ✅ Работа в условиях нестабильного интернета

**Особенности:**
- Использует флаг `--read-only` для защиты от записи
- Оптимизирован для быстрого листинга файлов

---

### 2. `--vfs-cache-mode minimal`
**Описание:** Кэшируется только файлы, открытые на запись. Чтение — потоковое.

**Параметры:**
```bash
--vfs-cache-mode minimal
--vfs-cache-max-size 1G
--vfs-read-chunk-size 64M
```

**Когда использовать:**
- ✅ Редкое редактирование файлов
- ✅ Ограниченное дисковое пространство
- ✅ Простые сценарии чтения

**Особенности:**
- Безопаснее для записей
- Меньше места, чем `writes`
- Чтение может быть медленным

---

### 3. `--vfs-cache-mode writes`
**Описание:** Все файлы, которые редактируются/создаются, сначала кэшируются локально, затем загружаются в облако.

**Параметры:**
```bash
--vfs-cache-mode writes
--vfs-cache-max-size 2G
--vfs-read-chunk-size 32M
--vfs-read-ahead 128M
--dir-cache-time 168h
```

**Когда использовать:**
- ✅ Работа с документами
- ✅ Редактирование файлов
- ✅ Хороший баланс между безопасностью и производительностью

**Особенности:**
- Требует места на диске для кэша
- Безопаснее для записей
- Подходит для большинства приложений

---

### 4. `--vfs-cache-mode full`
**Описание:** Максимальная совместимость. Полное кэширование содержимого и метаданных.

**Параметры:**
```bash
--vfs-cache-mode full
--vfs-cache-max-size 5G
--vfs-read-chunk-size 64M
--vfs-read-ahead 256M
--buffer-size 64M
--dir-cache-time 168h
--attr-timeout 1s
```

**Когда использовать:**
- ✅ Медиаплееры с произвольным доступом
- ✅ Сложные редакторы
- ✅ Приложения, ожидающие POSIX-семантику
- ✅ Работа с базами данных (не рекомендуется, но если очень нужно)

**Особенности:**
- Самое большое потребление диска/IO
- Максимальная производительность
- Самый безопасный вариант

---

## 📊 Параметры для разных сценариев

### Документы и офисные файлы
```bash
--vfs-cache-mode writes
--vfs-cache-max-size 2G
--vfs-read-chunk-size 32M
--vfs-read-ahead 128M
--dir-cache-time 168h
```

**Для чего:**
- Редактирование текстовых документов
- Работа с электронными таблицами
- Просмотр PDF
- Работа с офисными приложениями

---

### Медиафайлы (видео, музыка)
```bash
--vfs-cache-mode full
--vfs-cache-max-size 10G
--vfs-read-chunk-size 128M
--vfs-read-ahead 512M
--buffer-size 64M
--dir-cache-time 168h
```

**Для чего:**
- Просмотр видео
- Прослушивание музыки
- Работа с медиаплеерами
- Стриминг с seek

---

### Бэкапы и большие файлы
```bash
--vfs-cache-mode minimal
--vfs-cache-max-size 1G
--vfs-read-chunk-size 256M
--vfs-read-ahead 512M
--dir-cache-time 720h
```

**Для чего:**
- Загрузка больших файлов
- Резервное копирование
- Работа с архивами
- Минимальное использование ресурсов

---

### Смешанный сценарий (по умолчанию)
```bash
--vfs-cache-mode full
--vfs-cache-max-size 5G
--vfs-read-chunk-size 64M
--vfs-read-ahead 256M
--buffer-size 64M
--dir-cache-time 168h
--attr-timeout 1s
```

**Для чего:**
- Универсальное использование
- Когда не знаете что будете делать
- Хороший баланс всех параметров

---

## 🚀 Дополнительные оптимизации

### Параллельные операции
```bash
--transfers=8          # Количество параллельных загрузок
--checkers=8           # Количество параллельных проверок
--tpslimit=10          # Ограничение запросов в секунду (если нужно)
```

### Использование памяти
```bash
--use-mmap             # Использовать memory mapping
--async-read           # Асинхронное чтение
```

### Управление кэшем
```bash
# Проверить размер кэша
du -sh ~/.cache/rclone/

# Очистить кэш вручную
rm -rf ~/.cache/rclone/*

# Принудительно обновить кэш директорий
rclone rc vfs/refresh recursive=true remote=yandex_disk:
```

---

## 📈 Сравнение производительности

| Режим | Скорость чтения | Скорость записи | Потребление RAM | Потребление диска | Безопасность |
|-------|----------------|-----------------|-----------------|-------------------|--------------|
| **off** | ⭐⭐⭐⭐⭐ | ❌ | ⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **minimal** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **writes** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **full** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |

---

## 🛠️ Отладка производительности

### Медленный первый доступ к файлам
**Причина:** Нет кэша, происходит загрузка с облака

**Решение:**
```bash
--dir-cache-time=168h
--vfs-cache-max-size=10G
--vfs-read-ahead=512M
```

### Большое потребление памяти
**Причина:** Слишком большой буфер

**Решение:**
```bash
--buffer-size=16M
--vfs-cache-max-size=2G
--vfs-read-chunk-size=32M
```

### Медленный листинг больших директорий
**Причина:** Частые API-запросы

**Решение:**
```bash
--dir-cache-time=720h
--attr-timeout=5s
--vfs-cache-mode=full
```

### Проблемы с записью
**Причина:** Неправильный режим кэша

**Решение:**
```bash
--vfs-cache-mode=writes  # или full
```

---

## 🎯 Рекомендации по выбору

### Если у вас мало места на диске
```bash
--vfs-cache-mode minimal
--vfs-cache-max-size 1G
```

### Если нужна максимальная скорость
```bash
--vfs-cache-mode full
--vfs-cache-max-size 10G
--vfs-read-chunk-size 128M
--vfs-read-ahead 512M
```

### Если нужна максимальная безопасность
```bash
--vfs-cache-mode off
--read-only
```

### Если не знаете что выбрать
```bash
--vfs-cache-mode full
--vfs-cache-max-size 5G
--vfs-read-chunk-size 64M
--vfs-read-ahead 256M
```

---

## 📚 Связанные документы

- **[Безопасность](02_Безопасность.md)** - Правила безопасной работы
- **[Команды](03_Команды.md)** - Команды управления
- **[Системные сервисы](05_Системные_сервисы.md)** - Настройка systemd
- **[Быстрый старт](01_Быстрый_старт.md)** - Пошаговая настройка
- **[Готовые сценарии](../examples/usage_examples.md)** - Примеры использования

---

**Правило выбора:** Начните с `full` и 5G кэша. Если мало места - уменьшайте кэш или переходите на `writes`. Если только чтение - используйте `off`.