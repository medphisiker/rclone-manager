# Rclone Manager

**Утилита для безопасного управления облачными дисками через rclone с поддержкой read-only и read-write режимов**

---

## ⚠️ ВАЖНО: Безопасность данных

**При удалении файлов из смонтированных облачных дисков они удаляются напрямую из облака без возможности восстановления!**

| Режим | Безопасность | Описание |
|-------|--------------|----------|
| **read-only** | ✅ Безопасно | Только чтение, удаление невозможно |
| **read-write** | ⚠️ Опасно | Чтение и запись, удаление возможно |

**Правило безопасности:** Всегда используйте read-only режим по умолчанию. Переключайтесь в read-write только при необходимости и сразу возвращайтесь обратно.

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Установка
```bash
cd ~/projects/rclone-manager
sudo ./install.sh
```

### Шаг 2: Настройка rclone (если еще не настроено)
```bash
rclone config
# Создайте облака с именами: yandex_disk, mail_ru
```

### Шаг 3: Первое использование
```bash
# Подключить все облака в безопасном режиме
rmro

# Проверить статус
rms

# Готово! Теперь можно работать с файлами в ~/mnt/
```

---

## 📖 Основные команды

### Безопасные команды (всегда доступны)
```bash
rmro              # Все облака в read-only
rms               # Статус всех облаков
rmstop            # Остановить все облака
```

### Команды для записи (используйте осторожно)
```bash
rmrw              # Все облака в read-write
rmt               # Переключить режим всех облаков
```

### Для конкретного облака
```bash
rmro_yandex       # Яндекс Диск в read-only
rmrw_yandex       # Яндекс Диск в read-write
rmt_yandex        # Переключить Яндекс Диск
rms_yandex        # Статус Яндекс Диска
```

---

## 📁 Структура проекта

```
rclone-manager/
├── README.md                      # Эта документация
├── rclone_manager.sh              # Основной скрипт управления
├── install.sh                     # Скрипт установки
├── rclone@-ro.service             # Шаблон systemd (read-only)
├── rclone@-rw.service             # Шаблон systemd (read-write)
└── docs/                          # Подробная документация
    ├── Быстрый старт.md           # Пошаговый гайд
    ├── Безопасность.md            # Правила безопасной работы
    ├── Параметры VFS.md           # Сводная таблица параметров
    ├── Справочник.md              # Быстрый поиск команд
    └── Готовые сценарии.md        # Типовые задачи
```

---

## 🛡️ Безопасные рабочие потоки

### Поток 1: Чтение файлов
```bash
rmro              # Подключить в безопасном режиме
rms               # Проверить статус
ls ~/mnt/         # Работать с файлами
```

### Поток 2: Редактирование файлов
```bash
rmro              # Начать с read-only
rms               # Проверить статус
rmt yandex_disk   # Переключить только если нужно
# ... редактировать файлы ...
rmt yandex_disk   # Вернуть в read-only
rms               # Проверить что вернулись
```

---

## 🔍 Отладка и проверка

### Проверка статуса
```bash
rms               # Статус всех облаков
rms yandex_disk   # Статус конкретного облака
```

### Просмотр логов
```bash
# Логи всех облаков
ls ~/.cache/rclone/

# Лог конкретного облака
tail -f ~/.cache/rclone/rclone-yandex_disk-ro.log
```

### Проверка systemd
```bash
# Статус всех сервисов
systemctl --user list-units 'rclone@*'

# Статус конкретного сервиса
systemctl --user status rclone@yandex_disk-ro.service

# Просмотр логов сервиса
journalctl --user -u rclone@yandex_disk-ro.service -f
```

---

## 📚 Подробная документация

- **[Быстрый старт](docs/Быстрый%20старт.md)** - Пошаговая установка и настройка
- **[Безопасность](docs/Безопасность.md)** - Подробно о защите данных
- **[Системные сервисы](docs/Системные%20сервисы.md)** - Настройка systemd
- **[Параметры VFS](docs/Параметры%20VFS.md)** - Сводная таблица параметров
- **[Справочник](docs/Справочник.md)** - Быстрый поиск команд
- **[Готовые сценарии](examples/usage_examples.md)** - Типовые задачи

---

## ⚙️ Техническая информация

### Поддерживаемые облака
- **Yandex Disk** - `yandex_disk`
- **Mail.ru Cloud** - `mail_ru`

### Режимы VFS
- **Read-only**: `--vfs-cache-mode off` (минимальный кэш)
- **Read-write**: `--vfs-cache-mode full` (полное кэширование)

### Системные требования
- Linux с systemd
- rclone установлен
- FUSE поддержка

---

## 🤝 Поддержка и развитие

### Отладка проблем
Если что-то не работает:
1. Проверьте статус: `rms`
2. Посмотрите логи: `tail -f ~/.cache/rclone/rclone-*.log`
3. Проверьте конфигурацию: `rclone config show`

### Добавление нового облака
1. Настройте remote: `rclone config`
2. Создайте точку монтирования: `mkdir -p ~/mnt/new_cloud`
3. Используйте: `rclone_manager.sh ro new_cloud`

### Поддержка и развитие
- **Репозиторий:** https://github.com/medphisiker/rclone-manager
- **Отладка:** Проверьте статус `rms`, посмотрите логи `tail -f ~/.cache/rclone/rclone-*.log`

---

## 📄 Лицензия

MIT License - свободно используйте и модифицируйте.

---

**Безопасность прежде всего! Используйте read-only режим по умолчанию.** 🔒