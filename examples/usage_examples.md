# 📋 Готовые сценарии использования Rclone Manager

**Типовые задачи с готовыми командами.**

---

## Сценарий 1: Безопасное чтение файлов

**Цель:** Скачать или посмотреть файлы без риска их случайного удаления.

```bash
# 1. Подключить все облака в безопасном режиме
rmro

# 2. Проверить статус
rms

# 3. Работать с файлами
ls ~/mnt/yandex_disk
ls ~/mnt/mail_ru

# 4. Скопировать нужные файлы
cp ~/mnt/yandex_disk/photo.jpg ~/Downloads/
cp ~/mnt/mail_ru/document.txt ~/Documents/

# 5. Отключить (опционально)
rmstop
```

**Безопасность:** 100% - файлы нельзя удалить или изменить.

---

## Сценарий 2: Редактирование файлов с возвратом в безопасный режим

**Цель:** Отредактировать файлы и сразу вернуться в безопасный режим.

```bash
# 1. Подключить нужное облако в read-only
rmro yandex_disk

# 2. Проверить статус
rms yandex_disk

# 3. Переключить в режим записи (только если нужно)
rmt yandex_disk

# 4. Проверить что переключились
rms yandex_disk  # Должно показать READ-WRITE

# 5. Редактировать файлы
nano ~/mnt/yandex_disk/notes.txt
# или
echo "новое содержимое" > ~/mnt/yandex_disk/new_file.txt

# 6. Немедленно вернуться в безопасный режим
rmt yandex_disk

# 7. Проверить возврат
rms yandex_disk  # Должно показать READ-ONLY
```

**Безопасность:** Контролируемый риск - вы сразу возвращаетесь в безопасный режим.

---

## Сценарий 3: Работа с несколькими облаками одновременно

**Цель:** Управлять разными облаками с разными режимами.

```bash
# 1. Подключить все облака в read-only
rmro

# 2. Проверить статус
rms
# Вывод:
# yandex_disk: READ-ONLY (смонтирован)
# mail_ru: READ-ONLY (смонтирован)

# 3. Нужно отредактировать файл только в Яндекс Диске
rmt yandex_disk
# Вывод:
# yandex_disk: READ-WRITE (смонтирован)
# mail_ru: READ-ONLY (смонтирован)

# 4. Редактируем...
nano ~/mnt/yandex_disk/notes.txt

# 5. Возвращаем в read-only
rmt yandex_disk
# Вывод:
# yandex_disk: READ-ONLY (смонтирован)
# mail_ru: READ-ONLY (смонтирован)
```

**Безопасность:** Mail.ru оставался в read-only весь время.

---

## Сценарий 4: Быстрая проверка перед важной операцией

**Цель:** Убедиться в правильном режиме перед опасными операциями.

```bash
# Перед важной операцией (удаление, копирование больших файлов)
rms

# Если нужно удалять - убедитесь в read-write
# Если не в read-write - переключите
rmt yandex_disk

# Проверьте еще раз
rms yandex_disk

# Выполните операцию
rm ~/mnt/yandex_disk/temp.txt

# Вернитесь в безопасный режим
rmt yandex_disk

# Финальная проверка
rms
```

**Безопасность:** Множественные проверки минимизируют риск ошибки.

---

## Сценарий 5: Экстренная остановка

**Цель:** Быстро остановить все облака при проблемах.

```bash
# Вариант 1: Остановить все
rmstop

# Вариант 2: Остановить конкретное облако
rmstop yandex_disk

# Вариант 3: Через systemd
systemctl --user stop rclone@yandex_disk-ro.service
systemctl --user stop rclone@yandex_disk-rw.service

# Проверить что остановлено
rms
```

**Безопасность:** Немедленная остановка всех операций.

---

## Сценарий 6: Работа с конкретными облаками

**Цель:** Управлять отдельными облаками независимо.

```bash
# Подключить только Яндекс Диск в read-only
rmro yandex_disk
rms yandex_disk

# Подключить только Mail.ru в read-write
rmrw mail_ru
rms mail_ru

# Переключить только Яндекс Диск
rmt yandex_disk

# Остановить только Mail.ru
rmstop mail_ru
```

---

## Сценарий 7: Массовые операции с контролем

**Цель:** Работать со всеми облаками с максимальным контролем.

```bash
# 1. Подключение всех облаков
rmro
rms

# 2. Если нужно редактировать во всех - переключаем все
rmrw
rms

# 3. Работа...
# ...редактирование файлов во всех облаках...

# 4. Возврат в безопасный режим
rmro
rms
```

---

## Сценарий 8: Проверка и отладка

**Цель:** Убедиться что все работает корректно.

```bash
# Проверка установки
rclone_manager.sh st
alias | grep rm

# Проверка безопасности
rmro
touch ~/mnt/yandex_disk/test.txt
rm ~/mnt/yandex_disk/test.txt  # Должно быть отказано
echo "test" > ~/mnt/yandex_disk/test.txt  # Должно быть отказано

# Проверка логов
ls ~/.cache/rclone/
tail -f ~/.cache/rclone/rclone-yandex_disk-ro.log

# Проверка systemd
systemctl --user list-units 'rclone@*'
journalctl --user -u rclone@yandex_disk-ro.service -f

# Проверка монтирования
findmnt --target ~/mnt/yandex_disk
mount | grep rclone
df -h ~/mnt/yandex_disk
```

---

## 🚨 Что НЕ делать

### ❌ Критические ошибки

```bash
# ❌ НИКОГДА не делайте так:
rm -rf ~/mnt/yandex_disk/важная_папка
# В read-write режиме = УДАЛЕНИЕ ИЗ ОБЛАКА БЕЗ КОРЗИНЫ!

# ❌ НИКОГДА не запускайте без проверки:
rmrw              # Без необходимости

# ❌ НИКОГДА не игнорируйте статус:
# Перед важной операцией всегда проверяйте:
rms
```

### ❌ Опасные паттерны

```bash
# Опасно: массовое удаление
rm -rf ~/mnt/*

# Опасно: синхронизация без проверки
rclone sync ~/local ~/mnt/remote

# Опасно: неизвестный режим
# Всегда проверяйте перед операциями!
```

---

## ✅ Чек-лист безопасности

### Перед каждой сессией:

- [ ] **Проверьте режим**: `rms`
- [ ] **Если нужна запись**: переключили и снова проверили
- [ ] **После записи**: вернули в read-only
- [ ] **Финальная проверка**: `rms` показывает read-only

### Перед опасными операциями:

- [ ] **Что я хочу сделать?**
- [ ] **Какой режим нужен?**
- [ ] **Как вернуться в безопасный режим?**
- [ ] **Есть ли бэкап?**

---

## 💡 Быстрые команды

### Безопасные команды (всегда используйте)
```bash
rmro              # Подключить в безопасном режиме
rms               # Проверить статус
rmstop            # Остановить все
ls ~/mnt/         # Просмотр файлов
cp ~/mnt/ ~/Downloads/  # Копирование
```

### Команды для записи (используйте осторожно)
```bash
rmrw              # Подключить в режим записи
rmt               # Переключить режим
nano ~/mnt/file   # Редактирование
```

---

## 📞 Что делать, если...

### Случайно переключил в read-write?
```bash
# 1. Немедленно вернитесь в read-only
rmt yandex_disk

# 2. Проверьте что не удалили ничего важного
ls ~/mnt/yandex_disk

# 3. Если удалили - проверьте облако через веб-интерфейс
```

### Не помните в каком режиме?
```bash
rms               # Покажет текущий режим
```

### Нужно срочно остановить все?
```bash
rmstop            # Остановить все облака
```

---

## 📚 Документация

- **[Команды](../docs/01_Команды.md)** - Все команды управления
- **[Безопасность](../docs/02_Безопасность.md)** - Правила работы
- **[VFS параметры](../docs/03_VFS_Параметры.md)** - Настройки производительности
- **[Системные сервисы](../docs/04_Системные_сервисы.md)** - Настройка systemd
- **[Быстрый старт](../docs/05_Быстрый_старт.md)** - Установка и настройка
- **[Справочник](../docs/06_Справочник.md)** - Быстрый поиск информации

---

**Главное правило: После любой операции с записью немедленно возвращайтесь в read-only!** 🔒